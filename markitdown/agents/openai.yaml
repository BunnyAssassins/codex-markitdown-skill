interface:
  display_name: "MarkItDown"
  short_description: "Convert local documents into LLM-ready Markdown"
  default_prompt: "Use $markitdown to convert these local files into Markdown and verify the outputs."

